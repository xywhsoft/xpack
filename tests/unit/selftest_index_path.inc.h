	/* index mode / index lookup / index mutation semantics */
#include "index_mode.inc.h"

	/* win32 path mode / path normalization / path traversal */
#include "path_win32.inc.h"

	/* path lookup corruption / bad-format guards */
#include "path_lookup_guard.inc.h"

	/* linux path mode / case-sensitive path behavior */
#include "path_linux.inc.h"
