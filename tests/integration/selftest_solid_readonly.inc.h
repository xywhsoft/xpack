	/* solid mode / solid build / solid read semantics */
#include "solid_mode.inc.h"

	/* layout transitions / layout state guards / format sizing */
#include "layout_state.inc.h"

	/* readonly behavior / internal bad-state / cleanup regressions */
#include "readonly_guard.inc.h"
